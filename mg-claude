#!/bin/bash
# ============================================
# 🤖 MuayadGen المحلي - مع Claude API مباشرة
# ============================================

# الحصول على المسار الحقيقي للسكريبت (حتى لو كان symlink)
REAL_SCRIPT="$(readlink -f "$0")"
SCRIPT_DIR="$(dirname "$REAL_SCRIPT")"

cd "$SCRIPT_DIR"
node mg-local.js "$@"
